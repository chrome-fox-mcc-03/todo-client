<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include ('./partials/bootstrap-css.ejs') %>
    <title>Movie</title>
    <style>
    table {
        margin-left: 50px;
        border-collapse: collapse;
        width: 90%;
        border: 1px solid rgb(109, 107, 107);
    }
    /* table, td, th {
        border: 1px solid black;
    } */

    h1 {
        margin-top: 50px;
        margin-left: 50px;
    }

    th, td {
        text-align: left;
        padding: 8px;
    }

    th {
        background-color: rgb(146, 228, 243);
        color: black;
    }
    </style>
</head>
<body>
    <%- include ('./partials/navbar.ejs') %>
    <h1>Movies</h1>
    <a href="/movies/add" style="margin-left: 50px;" class="btn btn-success">Add Movie</a>
    <br>
    <br>
    <table>
        <tr>
            <th>Movie</th>
            <th>Released year</th>
            <th>Genre</th>
            <th>Production House</th>
            <th>Actions</th>
        </tr>
        <% for (let i = 0; i < movies.length; i++) {%>
            <tr>
                <td><%= movies[i].name%></td>
                <td><%= movies[i].released_year%></td>
                <td><%= movies[i].genre%></td>
                <td><%= movies[i].ProductionHouseId ? movies[i].ProductionHouse.name_prodHouse : 'unknown' %></td>
                <td><a href="/movies/update/<%= movies[i].id %>" class="btn btn-info">Edit</a>  <a href="/movies/delete/<%= movies[i].id %>" class="btn btn-info" onclick="return confirm('Are you sure you want to delete this Movie?')">Delete</a>  <a href="/movies/addcast/<%= movies[i].id %>" class="btn btn-info">Add Cast</a></td>
            </tr>
        <% } %>
    </table>
    <%- include ('./partials/bootstrap-js.ejs') %>
</body>
</html>